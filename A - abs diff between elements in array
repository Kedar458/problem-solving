def diff():
    arr=list(map(int,input().split()))
    num=int(input())
    diff=int(input())
    count=0
    for i in range(len(arr)):
        if abs(arr[i]-num)<=diff:
            count+=1
    return count
result=diff()
print(result)
